export type SkillsSet = {
    name: string,
    level: number,
}

export type TestimonialsInfo = {
    name: string,
    role: string,
    content: string,
    rating: number,
}

export type ProjectInfo = {
    title: string,
    description: string,
    image: string,
    techStack: string[],
    link: string,
}
